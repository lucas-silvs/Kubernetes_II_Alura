apiVersion: v1
kind: Service
metadata:
  name: svc-pod-2
spec:
  type: ClusterIP
  selector:
    app: segundo-pod
  #define a porta que vai ouvir (port) e a porta que vai despachar(targetPort)
  ports:
    - port: 9000
      targetPort: 80


    
  
  
